#!/usr/bin/env node

var program = require('commander');

program
  .version(require('./../package.json').version);

require('./../src/commands/make/component').load();
require('./../src/commands/new').load();

program.parse(process.argv);
