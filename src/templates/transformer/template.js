/* ============
 * {{ name }} Transformer
 * ============
 *
 * todo: add documentation here!
 */

import Transformer from './transformer';

export default class {{ name }}Transformer extends Transformer {

  static fetch(item) {
    return {
      //
    };
  }

  static send(item) {
    return {
      //
    };
  }
}
